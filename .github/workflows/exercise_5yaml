name: E5

on:
  schedule: 
    -cron: '*/5 * * * *'


jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - uses: ./ # Uses an action in the root directory
        id: repeater
        with:
          in-var: Eren


